# Using lukaszraczylo's pre-built image (fast deployment)
FROM ghcr.io/lukaszraczylo/tdlib-telegram-bot-api-docker/telegram-api-server:latest

# This image comes pre-configured
# File storage: /var/lib/telegram-bot-api
# Port: 8081 (default for telegram-bot-api)

EXPOSE 8081

# No custom CMD needed - image has built-in entrypoint
